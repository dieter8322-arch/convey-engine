# ADR Guide

`adr/` 用于记录关键技术决策，而不是承载完整设计。

建议格式：

- 文件名：`YYYY-MM-DD-topic.md`
- 内容包含：背景、决策、备选方案、取舍、影响范围

适合记录的主题：

- 为什么首版选择 PostgreSQL 作为调度状态源
- 为什么先做单体多角色模式，而不是多服务拆分
- 为什么优先支持 `shell` 和 `docker` 两类执行器
